//! Task System v2 — M04b：本文件已下沉到 `magi-conversation-runtime::usage_recording`。

pub(crate) use magi_conversation_runtime::usage_recording::{
    ModelUsageBinding, model_usage_binding_for_worker,
};
